# n8n Home Server Environment Configuration
# Copy this file to .env and fill in your secure values

# ============================================
# PostgreSQL Database Configuration
# ============================================
# Root user for database administration
POSTGRES_USER=root
POSTGRES_PASSWORD=CHANGE_ME_SECURE_PASSWORD_1

# Database name
POSTGRES_DB=n8n

# Non-root user for n8n application
POSTGRES_NON_ROOT_USER=n8n
POSTGRES_NON_ROOT_PASSWORD=CHANGE_ME_SECURE_PASSWORD_2

# ============================================
# Redis Configuration
# ============================================
# Password for Redis cache
REDIS_PASSWORD=CHANGE_ME_SECURE_PASSWORD_3

# ============================================
# Evolution API Configuration
# ============================================
# API key for Evolution API authentication
EVOLUTION_API_KEY=CHANGE_ME_SECURE_API_KEY

# Database connection URI for Evolution API
# Format: postgresql://username:password@host:port/database
EVOLUTION_DATABASE_URI=postgresql://n8n:CHANGE_ME_SECURE_PASSWORD_2@postgres:5432/n8n

# ============================================
# ngrok Configuration
# ============================================
# Your ngrok static domain (if you have a paid plan)
# If using free plan, this will change each time ngrok restarts
NGROK_DOMAIN=heather-unignited-unacquisitively.ngrok-free.dev

# ============================================
# SECURITY NOTES
# ============================================
# 1. Replace ALL "CHANGE_ME_*" values with strong, unique passwords
# 2. Use different passwords for each service
# 3. Generate secure passwords with: openssl rand -base64 32
# 4. Never commit this file to version control
# 5. Keep a backup of this file in a secure location
# 6. Passwords should be at least 20 characters long
# 7. Use a mix of letters, numbers, and special characters

# ============================================
# PASSWORD GENERATION EXAMPLES
# ============================================
# Run these commands to generate secure passwords:
#
# For POSTGRES_PASSWORD:
#   openssl rand -base64 32
#
# For POSTGRES_NON_ROOT_PASSWORD:
#   openssl rand -base64 32
#
# For REDIS_PASSWORD:
#   openssl rand -base64 32
#
# For EVOLUTION_API_KEY:
#   openssl rand -base64 32
#
# Then copy the generated values above and update EVOLUTION_DATABASE_URI

